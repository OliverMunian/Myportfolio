"use client";
//Assets
//Extensions
import { useEffect, useState, useRef } from 'react'
import Image from "next/image";
import { motion } from 'framer-motion'
import DecryptedText from "./DecryptedText";

export default function Projects() {
  return (
    <div className="relative size-full flex flex-col items-center justify-start overflow-hidden z-0">
      <h1 className="text-white font-extrabold text-[220px]">
        PROJECTS
      </h1>
    </div>
  );
}
